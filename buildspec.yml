version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm ci --legacy-peer-deps

  build:
    commands:
      - echo Building the React app...
      - npm run build
  test:
    commands:
      - echo checking the tests in React app...
      - npm run test

artifacts:
  files:
    - '**/*'
  base-directory: build
  discard-paths: no
